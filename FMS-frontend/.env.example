# Copy this to .env before running the dev server.

# API Gateway URL — all REST microservices route through here
VITE_API_URL=http://localhost:8080/api

# Message Service WebSocket URL — connects directly to message-service
# (the gateway proxies WebSocket at /ws/** but direct connection is more reliable)
VITE_WS_URL=http://localhost:8090/ws
